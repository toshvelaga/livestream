const { default: axios } = require('axios')
require('dotenv').config()

const bindYoutubeBroadcastToStream = async (youtubeAccessToken) => {
  return { msg: 'this is it' }
}

module.exports = bindYoutubeBroadcastToStream
